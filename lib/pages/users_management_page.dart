// /lib/pages/users_management_page.dart

import 'package:flutter/material.dart';

class UsersManagementPage extends StatelessWidget {
  const UsersManagementPage({super.key});

  @override
  Widget build(BuildContext context) {
    // Aquí se leerán los datos de AppStateProvider y se mostrará la tabla de usuarios.
    return const Center(
      child: Text("Gestión de Usuarios (Tabla y CRUD forms)"),
    );
  }
}
